#========================================================================
# General
#========================================================================

# Logs and databases
*.log
*.sql
*.sqlite
*.sqlite3

# Environment variables
.env

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

#========================================================================
# IDE Files (Visual Studio, Rider, VS Code)
#========================================================================

# Visual Studio / Rider specific
*.suo
*.user
*.userosscache
*.sln.docstates
.vs/            # Visual Studio cache directory
.idea/          # Rider/JetBrains cache directory

# VS Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

#========================================================================
# .NET / C# Build Artifacts
appsettings.json
appsettings.Development.json
#========================================================================

# Binaries, build outputs, NuGet packages
*.dll
*.exe
*.pdb
*.nupkg
*.snupkg
App.config      # Unless you have specific reasons to version it

# Build directories (case-insensitive to cover 'bin' and 'Bin', 'obj' and 'Obj')
[Bb]in/
[Oo]bj/

# NuGet packages folder (less common with SDK-style projects but good to keep)
[Pp]ackages/

# NuGet lock file (optional, depends on team policy - uncomment if you don't want to commit it)
# packages.lock.json

# Rider specific build cache
*.csproj.DotSettings.user

# Resharper specific
_ReSharper*/
*.[Rr]esharper
*.DotSettings.user

# Test results
[Tt]est[Rr]esult*/
[Bb]uild[Ll]og.*
*.[Tt]est[Rr]esult
*.VisualState.xml

# Profiler results
*.psess
*.vsp
*.vspx
*.sap

#========================================================================
# Avalonia Specific (Usually covered by bin/obj, but just in case)
#========================================================================
# Generally, standard .NET ignores cover Avalonia build outputs.
# Add specific patterns here ONLY if you find Avalonia generating unique
# temporary files outside bin/obj that aren't covered above.

#========================================================================
# JavaScript / Node (For your Browser Extension)
#========================================================================
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dist/           # Common build output directory for JS projects
.npm

#========================================================================
# Other Tools
#========================================================================

# Doco generated files
API/

# Rider auto generated file upon startup in directory
.idea/.idea.Gamification/.idea/workspace.xml